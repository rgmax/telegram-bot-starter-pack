const telegram = require('./telegram.json')
const defaults = require('./defaults.json')
const messages = require('./messages.json')
const services = require('./services.json')

module.exports = {telegram, defaults, messages, services}
